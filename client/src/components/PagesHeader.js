// const PagesHeader = ({ title }) => {
const PagesHeader = () => {

    return (
        <div className='pages-header'>
            PagesHeader.js
        </div>
    );
};

export default PagesHeader;
